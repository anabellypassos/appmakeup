import 'package:flutter/material.dart';

class ProdutoCategorias extends StatelessWidget {
  const ProdutoCategorias({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ProdutoCategorias '),
      ),
      body: const Center(
        child: Text('ProdutoCategorias '),
      ),
    );
  }
}
